<Window x:Class="ASPAssistant.App.Windows.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="输入"
        Width="460" Height="240"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource PanelBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        Icon="{StaticResource AspaLogo}">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="0" GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="#FF12122A" MouseLeftButtonDown="OnTitleBarDrag">
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="✕" Click="OnCancelClick"
                            Style="{StaticResource CloseButton}" />
                    <TextBlock x:Name="TitleText" Text="输入"
                               Foreground="{StaticResource TextPrimaryBrush}" FontSize="13"
                               FontWeight="SemiBold" Padding="12,8" VerticalAlignment="Center" />
                </DockPanel>
            </Border>

            <Border DockPanel.Dock="Bottom" Background="#FF12122A" Padding="12,10"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Click="OnCancelClick"
                            Padding="14,4" FontSize="12" Cursor="Hand" Margin="0,0,8,0"
                            Background="#22FFFFFF" BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1" Foreground="{StaticResource TextPrimaryBrush}" />
                    <Button Content="确定" Click="OnOkClick" IsDefault="True"
                            Padding="14,4" FontSize="12" Cursor="Hand"
                            Background="#FF3D6EF5" BorderThickness="0" Foreground="White" />
                </StackPanel>
            </Border>

            <StackPanel Margin="14,12">
                <TextBlock x:Name="PromptText" Text="" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" TextWrapping="Wrap" />
                <TextBox x:Name="InputBox"
                         Background="#22000000" Foreground="{StaticResource TextPrimaryBrush}"
                         BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                         CaretBrush="{StaticResource AccentBrush}"
                         Padding="4,6" FontSize="13"
                         AcceptsReturn="False"
                         TextWrapping="NoWrap" />
            </StackPanel>
        </DockPanel>
    </Border>
</Window>
